/** Stable ID for the system Inbox folder. */
export const INBOX_FOLDER_ID = 'inbox';

/** Maximum recording duration in milliseconds (10 minutes). */
export const MAX_RECORDING_DURATION_MS = 10 * 60 * 1000;
